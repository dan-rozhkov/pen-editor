Ты — инициализатор проекта. Твоя задача — **только** подготовить окружение для долгосрочной работы.

1. Прочитай план в ~/.claude/plans/polished-cuddling-fairy.md.

2. Создай подробный feature_list.json — список **всех** необходимых end-to-end фич в формате JSON array. Каждая фича — объект с полями: category, description, steps (массив шагов проверки), passes: false.

3. Создай claude-progress.txt с начальной записью.

4. Сделай git init → add → commit "Init project".

Больше ничего не делай.
