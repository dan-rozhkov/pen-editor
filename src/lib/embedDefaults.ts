/**
 * Default HTML content for a freshly created embed node — used by both the
 * embed draw tool (src/pixi/interaction/drawController.ts) and the embed
 * size-presets panel (src/components/PropertiesPanel.tsx) so the two paths
 * can't drift apart.
 */
export const DEFAULT_EMBED_HTML =
  '<div style="padding: 16px; font-family: sans-serif; color: #333;"><h2 style="margin: 0 0 8px 0;">HTML Embed</h2><p style="margin: 0;">Edit HTML content in the properties panel.</p></div>';
